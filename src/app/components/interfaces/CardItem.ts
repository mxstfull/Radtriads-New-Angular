export interface CardItem {
  unique_id: string;
  title: string,
  url: string,
  filename: string,
  diskspace: number,
  category: number, 
  is_protected: number,
  created_at: string,
  updated_at: string,
}